# RAG 完整流程与主流方法总结

> 基于文件夹内全部 PDF 文档内容核实，结合 2025-2026 主流实践整理。内容经过逐章验证。

---

## 一、RAG 概述

RAG（Retrieval-Augmented Generation，检索增强生成）通过"先检索相关文档，再基于文档生成答案"的两阶段架构，有效缓解了 LLM 的知识陈旧、幻觉和领域知识不足问题。

### 1.1 演进路线

```
Naive RAG → Advanced RAG → Modular RAG → Agentic RAG → Multi-Agent RAG
```

| 阶段 | 核心特征 | 代表技术 | 适用场景 |
|------|----------|----------|----------|
| Naive RAG | 向量检索 + 直接生成，单次往返 | 基础 FAISS + GPT | FAQ 机器人、简单查找 |
| Advanced RAG | 查询优化、混合检索、重排序 | HyDE、Hybrid Search、Reranker | 大多数生产级知识系统 |
| Modular RAG | 模块化编排，灵活组合检索与生成策略 | LangChain/LlamaIndex Pipeline | 需要定制化的中等复杂系统 |
| Agentic RAG | LLM 自主决策检索策略，多轮迭代 | ReAct Loop、工具调用 | 多跳推理、复杂企业任务 |
| Multi-Agent RAG | 多智能体协作，并行检索与反思 | HM-ARAG、ADORE | 跨域推理、高精度场景 |

### 1.2 RAG 核心流程总览

RAG 分为两大阶段，外加评估闭环：

```
离线索引：数据加载 → 文本分块 → 向量嵌入 → 向量数据库 → 索引优化
在线查询：查询重构 → 查询构建 → 查询路由 → 混合检索 → 重排序 → 上下文压缩 → 生成
闭环反馈：评估 → 监控 → 持续优化
```

---

## 二、离线索引阶段

### 2.1 数据加载（Data Loading）

**核心任务：** 将各类非结构化文档（PDF、Word、Markdown、HTML 等）转换为程序可处理的结构化数据。

**核心原则：** "垃圾进，垃圾出"——数据质量直接决定 RAG 系统上限。

#### 主流工具

| 工具 | 特点 | 适用场景 |
|------|------|----------|
| **LlamaParse** | PDF→Markdown，OCR + 表格识别，深度结构解析 | 科研文献、技术手册 |
| **Unstructured** | 15+ 文档元素类型识别（Title/NarrativeText/Table/Image/Formula 等），统一 API | 混合格式文档库 |
| **Docling** (IBM) | 模块化企业级解析，与 Watsonx 深度集成 | 企业合同、报告 |
| **PyMuPDF4LLM** | PDF→Markdown，轻量高效 | PDF 转 Markdown 流 |
| **Marker** | PDF→Markdown，GPU 加速，Surya OCR 引擎驱动 | 科研文献批量处理 |
| **MinerU** | 多模态集成解析（LayoutLMv3 + YOLOv8），数学公式支持 | 学术文献、财务报表 |
| **FireCrawl** | 网页内容抓取，LLM 友好输出 | 在线文档、新闻 |

**选型建议：**
- PDF 密集型：LlamaParse（付费）或 Marker/MinerU（免费自部署）
- 混合格式+大规模：Unstructured
- 网页内容：FireCrawl

---

### 2.2 文本分块（Text Chunking）

**核心任务：** 将长文档切分为大小合适、语义连贯的块，平衡嵌入模型窗口限制和上下文利用效率。

**产业共识：** 分块策略是 RAG 系统中杠杆效应最高的优化环节。

#### 2.2.1 六种主流分块策略

| 策略 | 原理 | 优点 | 缺点 | 适用场景 |
|------|------|------|------|----------|
| **固定大小分块** | 按 chunk_size + chunk_overlap 切分，优先保持段落完整性 | 简单高效 | 可能在语义边界切断 | 快速原型、日志文件 |
| **递归字符分块** | 段落→句子→词→字符层次化分隔符递归切分 | **最通用**，支持多语言和代码 | 对弱结构文档效果一般 | **默认首选方案** |
| **语义分块** | 计算相邻句子嵌入的余弦相似度，在"跳跃点"切分 | 语义连贯性最佳 | 计算成本高 | 对话记录、主题松散文档 |
| **结构感知分块** | 利用 Markdown 标题层级、HTML 标签等文档结构 | 保留文档层次结构 | 强依赖文档结构化程度 | API 文档、技术手册 |
| **Agentic Chunking** | 使用 LLM 智能判断分块边界 | 语义连贯性最高 | 昂贵，不可大规模扩展 | 高价值小规模文档集 |
| **Late Chunking** | 先嵌入整篇文档再 token 级切分并池化 | 代词指代保留 | 需特定模型支持 | 指代密集的长文档 |

#### 2.2.2 递归字符分块详解（默认首选）

**算法流程：**
1. 从分隔符列表 `["\n\n", "\n", " ", ""]` 中从前到后遍历，找到第一个在文本中存在的分隔符
2. 用选定分隔符切分文本，不超过 chunk_size 的片段暂存，超过的用更细粒度分隔符递归切分
3. 通过 `_merge_splits` 合并合格片段，应用 chunk_overlap 重叠机制保持上下文连续性

**多语言支持：** 中文/日文等无词边界语言需添加 `"。"` `"，"` `"\uff0c"` `"\u3002"` 等分隔符。

**代码特化：** `RecursiveCharacterTextSplitter.from_language(language=Language.PYTHON)` 可按类/函数定义切分。

#### 2.2.3 语义分块详解

**SemanticChunker 工作流程：**
1. **句子分割：** 基于句号、问号、感叹号拆分句子
2. **上下文感知嵌入：** 通过 buffer_size 参数将每个句子与前后各 N 句组合后嵌入，融入上下文语义
3. **计算语义距离：** 相邻句子嵌入向量间的余弦距离，量化语义差异
4. **识别断点：** 按统计方法（percentile/standard_deviation/interquartile）确定动态阈值
5. **合并成块：** 在断点处切分句子序列，合并为语义连贯的文本块

#### 2.2.4 企业级分块架构：层次化父子分块

**2025 年生产环境黄金标准：**

```
查询 → 搜索子块索引（200-400 tokens，精准检索）
    → 通过 parent_id 获取父块（800-1000 tokens，丰富上下文）
    → 提交给 LLM
```

- **子块：** 检索精度优先，200-400 tokens
- **父块：** 上下文丰富度优先，800-1000 tokens

#### 2.2.5 分块大小选择

| 查询类型 | 推荐块大小 | 推荐重叠 |
|----------|----------|----------|
| 事实性/精确查找 | 64-128 tokens | 0-10% |
| 一般叙述性内容 | 300-800 tokens | 10-20% |
| 推理/论证类 | 512-1024 tokens | 15-25% |
| 代码/FAQ | 200-400 tokens | 0% |

#### 2.2.6 LlamaIndex 节点解析器

| 解析器 | 用途 |
|--------|------|
| **SentenceWindowNodeParser** | 句子级索引 + 前后 N 句窗口元数据 |
| **SemanticSplitterNodeParser** | 基于语义断点 |
| **MarkdownNodeParser / JSONNodeParser / CodeSplitter** | 结构感知型 |
| **TokenTextSplitter / SentenceSplitter** | 常规分块 |

---

### 2.3 向量嵌入（Vector Embedding）

**核心任务：** 将文本/图像转换为低维稠密数值向量，语义相似的对象在向量空间中距离更近。

**核心度量方法：**
- **余弦相似度：** 计算向量夹角余弦值，越接近 1 越相似（最常用）
- **点积：** 归一化后等价于余弦相似度
- **欧氏距离：** 直线距离，越小越相似

#### 2.3.1 嵌入技术发展历程

| 阶段 | 代表模型 | 核心突破 |
|------|----------|----------|
| 静态词嵌入 (2013-2014) | Word2Vec, GloVe | 一词一向量，奠基之作 |
| 动态上下文嵌入 (2018-2019) | BERT, RoBERTa | 同一词在不同语境中生成不同向量 |
| RAG 时代嵌入 (2022-2023) | Instructor, E5, BGE | 任务指令引导嵌入，领域自适应 |
| 多粒度多语言 (2024) | BGE-M3 | 密集+稀疏+多向量三合一，100+ 语言 |
| 原生多模态 (2025-2026) | Gemini Embedding 2, Harrier-OSS | 图文音视PDF统一向量空间 |

#### 2.3.2 核心训练技术

- **掩码语言模型（MLM）：** 随机遮盖 token 让模型预测，学习深层语境语义
- **对比学习（Contrastive Learning）：** 三元组训练（Anchor, Positive, Negative），拉近正例、推远负例
- **度量学习（Metric Learning）：** 直接优化正负例对在向量空间中的相对距离

#### 2.3.3 主流嵌入模型对比

| 模型 | 维度 | 上下文 | 多语言 | 开源 | 核心优势 |
|------|------|--------|--------|------|----------|
| **BGE-M3** (BAAI) | 1024 | 8192 | 100+语言 | MIT | **密集+稀疏+多向量三合一，自部署首选** |
| **Qwen3-Embedding-8B** | 32-4096 | 8192 | 多语言 | Apache 2.0 | 最强开源通用 |
| **Gemini Embedding 2** | 3072 | 32K+ | 多模态 | 闭源 | 原生多模态 SOTA |
| **OpenAI text-embedding-3-large** | 3072 | 8191 | 多语言 | 闭源 | 生态最完善 |
| **Cohere Embed v4** | 1536 | 128K | 100+ | 闭源 | 最长上下文 |

> 注：MTEB v1 与 v2 评分体系不同，分数不可直接跨版本对比。在生产场景中，在自己的私有评测集上迭代测试是找到最佳模型的唯一可靠方法。

#### 2.3.4 BGE-M3 深度解析（自部署首选）

BGE-M3 同时支持三种检索模式：
- **Dense（密集向量）：** 传统语义相似度检索
- **Sparse（稀疏向量）：** BM25 级关键词精确匹配
- **Multi-Vector（多向量）：** ColBERT 风格的 Token 级后期交互

**优势：** 568M 参数，MIT 协议，单 GPU 即可部署，100+ 语言。

#### 2.3.5 多模态嵌入

**为什么需要：** 文本向量和图像向量处于隔离空间，存在"模态墙"。多模态嵌入将不同类型数据映射到同一共享向量空间。

**CLIP 模型：** 双编码器架构（图像编码器 + 文本编码器），通过对比学习训练——最大化正确图文对的向量相似度，最小化错误配对的相似度。

---

### 2.4 向量数据库（Vector Database）

**核心任务：** 高效存储和检索海量高维向量，支持毫秒级相似性搜索。

**与传统数据库的核心差异：** 传统数据库擅长结构化数据精确匹配，向量数据库专为高维向量相似性搜索设计，采用 ANN 索引实现毫秒级查询。

#### 2.4.1 核心索引算法

| 算法 | 原理 | 优点 | 缺点 | 适用规模 |
|------|------|------|------|----------|
| **FLAT** | 暴力搜索，计算所有向量距离 | 100% 召回率 | 速度慢，内存大 | 百万级以下 |
| **IVF 系列** (IVF_FLAT/SQ8/PQ) | 聚类分桶，只搜索最近 nprobe 个桶 | 精度/速度平衡好 | 召回率非 100% | 千万至亿级 |
| **HNSW** | 多层邻近图，从稀疏上层快速定位到稠密下层 | **低延迟，高召回** | 内存占用极大 | 内存可容纳的数据集 |
| **DiskANN** | 基于 SSD 的高性能图索引 | 支持远超内存的十亿级数据 | 延迟略高于纯内存 | 十亿级以上 |
| **PQ（乘积量化）** | 向量压缩，低精度换速度 | 大幅减内存 | 召回率有损 | 存储敏感场景 |

#### 2.4.2 生产环境索引选择

| 场景 | 推荐索引 |
|------|----------|
| 低延迟、数据可全载入内存 | **HNSW** |
| 高吞吐、通用场景 | **IVF_FLAT / IVF_SQ8** |
| 数据量巨大、无法载入内存 | **DiskANN** |
| 小数据量、追求 100% 准确 | **FLAT** |

#### 2.4.3 Milvus 深度介绍

Milvus 是 Zilliz 开源的云原生向量数据库，LF AI & Data 基金会顶级项目。

| 概念 | 类比 | 说明 |
|------|------|------|
| **Collection** | 数据库表 | 所有数据的顶层容器 |
| **Partition** | 表分区 | 逻辑隔离（如按日期/类别），最多 1024 个 |
| **Schema** | 表结构定义 | 主键 + 向量字段 + 标量字段 |
| **Entity** | 一行数据 | 实际存储的数据 |
| **Alias** | 别名 | 支持零停机数据切换 |

**向量索引内部组件：** 数据结构（索引骨架）+ 量化（可选，数据压缩）+ 结果精炼（可选，精确计算）

**部署方式：** Docker Compose（单机）、Kubernetes（分布式）、Zilliz Cloud（全托管）

#### 2.4.4 主流向量数据库对比

| 产品 | 核心优势 | 适用场景 |
|------|----------|----------|
| **Milvus** | GPU 加速，稀疏+密集混合检索原生支持 | 大规模高性能部署 |
| **Pinecone** | 全托管 Serverless，99.95% SLA | 企业生产环境 |
| **Qdrant** | Rust 高性能，二进制量化 | 性能敏感应用 |
| **Weaviate** | 20+ AI 模块集成，原生多模态 | AI 快速原型 |
| **Elasticsearch** | 统一 BM25 + 向量检索 | 已有 ES 基础设施的组织 |
| **Chroma** | 零配置，本地优先 | 原型开发 |
| **FAISS** (Meta) | 极致性能，灵活度高 | 快速验证、研究 |

---

### 2.5 索引优化（Index Optimization）

在基础索引之上，通过结构化策略进一步提升检索质量。

#### 2.5.1 句子窗口检索（Sentence Window Retrieval）

**核心思想：** 为检索精确性而索引小块，为上下文丰富性而检索大块。

**实现流程：**
1. **索引阶段：** 文档拆分到句子级，每个句子作为独立节点存入向量数据库；同时在元数据中存储前后 N 句的窗口文本（窗口文本不被索引，仅作元数据存储）
2. **检索阶段：** 在所有句子节点上执行相似度搜索，精准定位最相关句子
3. **后处理阶段：** 用 `MetadataReplacementPostProcessor` 将检索到的句子替换为元数据中的完整窗口
4. **生成阶段：** 包含丰富上下文的节点传递给 LLM 生成答案

**优势：** 同时获得检索精度和生成上下文丰富度。

#### 2.5.2 元数据过滤（结构化索引）

- 为文本块附加元数据（文件名、日期、作者、章节标题、类别、语言等）
- 检索时先通过元数据预过滤缩小范围，再执行向量搜索
- 对多租户、多版本文档管理系统**必不可少**

#### 2.5.3 递归检索（Recursive Retrieval）

适用于多数据源场景：
1. 先为每个数据源创建摘要节点
2. 检索时先定位到正确的数据源
3. 在目标数据源内精确查询

#### 2.5.4 企业级分层知识库设计

```
Layer 1 — 核心静态层：版本化文档、手册、法律文本
    └─ 受控发布，全量重建 + 多版本快照

Layer 2 — 频繁更新层：更新日志、定价规则、产品变更
    └─ 定时增量更新，漂移监控

Layer 3 — 按需实时层：实时状态、API 返回数据
    └─ 查询时实时获取，结构化 API 调用
```

---

## 三、在线查询阶段

### 3.1 查询重构（Query Translation）

**核心任务：** 弥合用户自然语言提问与文档库中存储信息之间的"语义鸿沟"。

#### 主流技术

| 技术 | 原理 | 适用场景 |
|------|------|----------|
| **提示工程** | 引导 LLM 将查询改写为更清晰的检索形式，或生成结构化查询指令（如 JSON 排序指令） | 需要排序、比较、聚合的查询 |
| **多查询分解（Multi-Query）** | 将复杂问题拆分为多个子问题，分别检索后合并去重 | 包含多个子主题的问题 |
| **退步提示（Step-Back Prompting）** | 先生成更高层次的"退步问题"检索通用原理，再进行具体推理 | 需要原理支撑的细节问题 |
| **HyDE** | 用 LLM 生成"理想答案"的假设性文档，再用假设文档的向量检索真实文档 | 查询简短、文档详实的场景 |

**HyDE 详解：**
1. **生成：** LLM 根据查询生成假设性答案文档
2. **编码：** 将假设性文档输入编码器，生成向量
3. **检索：** 用假设性文档的向量在数据库中进行相似性搜索

HyDE 将困难的"查询→文档"匹配转化为相对容易的"文档→文档"匹配。

---

### 3.2 查询构建（Query Construction）

**核心任务：** 将自然语言翻译为针对特定数据源的结构化查询。

| 技术 | 用途 | 代表工具 | 关键优化 |
|------|------|----------|----------|
| **文本→元数据过滤器** | 从自然语言提取结构化过滤条件，语义搜索 + 元数据过滤 | SelfQueryRetriever | 清晰的元数据描述，temperature=0 |
| **文本→Cypher** | 翻译为图数据库查询语言，查询实体关系路径 | GraphCypherQAChain | 精确提供 Schema |
| **文本→SQL** | 翻译为 SQL 语句，查询关系型数据库 | RAGFlow 方案、DIN-SQL | 见下方四大策略 |

**Text-to-SQL 四大优化策略：**
1. **提供精确的 DDL：** 给 LLM 表结构、列名、数据类型和外键关系
2. **Few-shot 示例：** 提供少量高质量"问题-SQL"示例对
3. **语义知识库增强：** 为表/字段建立描述、同义词、业务术语映射，用向量检索相关信息注入 Prompt
4. **错误修正反思：** 执行报错后反馈给 LLM，迭代修复（最大重试 3 次效果最佳）

---

### 3.3 查询路由（Query Routing）

**核心任务：** 根据查询意图智能分发到最合适的数据源或处理组件。

**三大路由场景：**
- **数据源路由：** 产品文档 → 向量库 / 用户数据 → SQL 数据库 / 投资关系 → 知识图谱
- **组件路由：** 简单 FAQ → 向量检索 / 复杂任务 → Agent / 需要计算 → 工具调用
- **提示模板路由：** 数学 → 分步思考模板 / 代码 → 代码优化模板 / 通用 → 标准模板

**实现方法：**
- 基于 LLM 意图识别：设计路由选项 Prompt → LLM 分类 → 代码分支分发（LangChain RunnableBranch）
- 基于关键词/规则：适用于简单明确的路由场景，延迟最低

---

### 3.4 混合检索（Hybrid Search）

**核心思想：** 结合稀疏向量（关键词精确匹配）和密集向量（语义理解）。**纯单一检索方式已不被视为生产级方案。**

#### 稀疏向量 vs 密集向量

| 特性 | 稀疏向量（BM25/TF-IDF） | 密集向量（BERT/GPT Embeddings） |
|------|------------------------|-------------------------------|
| 表示 | 高维，绝大多数为零，每维对应一个词 | 低维稠密浮点数组 |
| 优势 | 精确关键词匹配，可解释，无需训练 | 语义理解，同义词泛化 |
| 劣势 | 词汇鸿沟（"汽车"≠"轿车"） | 可解释性差，需大量训练数据 |
| 搜索方向 | 精准匹配 | 模糊语义 |

**BM25 核心公式：**

$$Score(Q, D) = \sum_{i=1}^{n} IDF(q_i) \cdot \frac{f(q_i, D) \cdot (k_1 + 1)}{f(q_i, D) + k_1 \cdot (1 - b + b \cdot \frac{|D|}{avgdl})}$$

其中 $IDF(q_i)$ 为逆文档频率，$f(q_i, D)$ 为词频，$k_1$ 控制词频饱和度，$b$ 控制文档长度归一化。

#### 融合策略

| 策略 | 公式 | 特点 |
|------|------|------|
| **RRF（倒数排序融合）** | RRF_score(d) = Σ 1/(rank_i(d) + k)，k=60 | 只关心排名，无需归一化得分，零样本 |
| **加权线性组合** | Hybrid = α × Dense + (1-α) × Sparse | 灵活调节语义/关键词贡献比例 |

**生产环境典型权重：α = 0.7（70% 语义 + 30% 关键词）**

---

### 3.5 重排序（Re-ranking）

对初检结果进行精细再排序，提升 Top-K 精度。通常增加 100-300ms 延迟，但仅占总延迟的 10-15%。

| 方法 | 原理 | 精度 | 成本 | 延迟 | 适用场景 |
|------|------|------|------|------|----------|
| **RRF** | 融合多个检索器的排名 | 基线 | 零成本 | 极低 | 多路召回融合 |
| **RankLLM** | LLM 直接对候选排序打分 | 高 | API 调用费 | 中 | 高价值语义场景 |
| **Cross-Encoder** | Query+Doc 拼接输入 Transformer，输出单一分数 | **最高** | N 次模型推理 | **最高** | 精度要求最高的场景 |
| **ColBERT** | 独立编码后在 Token 级交互（MaxSim 聚合） | 高 | 中 | 中 | **精度与效率最佳平衡** |

**常用轻量 Cross-Encoder：** ms-marco-MiniLM-L-12-v2、ms-marco-TinyBERT-L-2-v2

**生产实践：** 混合检索召回 Top-50 → Cross-Encoder 重排到 Top-5，精度提升 30-40%

---

### 3.6 上下文压缩（Context Compression）

去除检索结果中的噪声，只保留与查询直接相关的信息：

| 方法 | 原理 | 适用 |
|------|------|------|
| **LLMChainExtractor** | 用 LLM 从每个文档只提取相关句子 | 内容提取 |
| **LLMChainFilter** | LLM 判断整篇是否相关，不相关整篇丢弃 | 文档过滤 |
| **EmbeddingsFilter** | 计算查询与文档向量相似度，低于阈值丢弃 | 低成本快速过滤 |

**LangChain 统一接口：** `ContextualCompressionRetriever` 包装基础检索器 + DocumentCompressor

---

### 3.7 生成（Generation）

**核心任务：** 将 LLM 的非结构化文本输出转换为结构化数据（JSON/XML），连接自然语言理解与下游程序化逻辑。

#### 3.7.1 LangChain Output Parsers

| 解析器 | 用途 | 严格度 |
|--------|------|--------|
| **StrOutputParser** | 返回原始文本字符串 | 低 |
| **JsonOutputParser** | 解析复杂 JSON（嵌套+列表） | 中 |
| **PydanticOutputParser** | 基于 Pydantic 模型最严格定义和验证，自动注入格式指令 | **高** |

**PydanticOutputParser 工作原理：** 将用户定义的 Pydantic 数据模型转换为详细的格式指令注入 Prompt → 引导 LLM 生成严格符合该数据结构的 JSON → 将 JSON 字符串安全解析为 Pydantic 对象实例。

#### 3.7.2 LlamaIndex 方案

- **响应合成器（Response Synthesizer）：** refine（逐块迭代优化）、compact（压缩填充）等模式
- **Pydantic Programs：** 优先使用 Function Calling，回退到 JSON Schema 注入

#### 3.7.3 Function Calling（Tool Calling）

- 模型不直接生成答案，而是返回工具调用指令
- 代码执行工具后，将结果返回模型进行二次生成
- 可靠性远高于纯文本 JSON 解析，是构建 Agent 的核心基础

#### 3.7.4 不依赖框架的实现思路

- 在 Prompt 中明确要求 JSON 格式，给出 JSON Schema
- Few-shot 示例（1-2 个完整"输入→输出"范例）
- GBNF 语法文件约束（llama.cpp 等本地部署最严格方法）

---

## 四、评估与监控

### 4.1 RAG 评估三元组

| 维度 | 评估目标 | 核心问题 |
|------|----------|----------|
| **上下文相关性** | 检索器性能 | 检索到的内容是否与查询高度相关？ |
| **忠实度** | 生成器可靠性 | 生成答案是否完全基于上下文？量化"幻觉"程度 |
| **答案相关性** | 端到端表现 | 最终答案是否直接、完整、有效地回答了问题？ |

### 4.2 检索评估指标

| 指标 | 公式 | 关注点 |
|------|------|--------|
| **Precision@k** | 前k个中相关数 / k | 准确性 |
| **Recall@k** | 前k个中相关数 / 全部相关总数 | 完整性 |
| **F1-Score** | 2×P×R/(P+R) | 综合平衡 |
| **MRR** | (1/\|Q\|) × Σ(1/rank_1st_relevant) | 首条相关排名 |
| **MAP** | (1/\|Q\|) × Σ AP(q) | 综合排名+精确率 |

### 4.3 响应评估方法

**基于 LLM 的评估（当前主流）：**
- 忠实度：答案分解为独立断言，逐条验证是否被上下文支撑
- 答案相关性：评估答案是否切题、完整，惩罚答非所问

**基于词汇重叠的经典指标：**

| 指标 | 侧重点 | 特点 |
|------|--------|------|
| **ROUGE** | 召回率导向 | 关注"说全了没" |
| **BLEU** | 精确率导向 | 含长度惩罚，"说对了没" |
| **METEOR** | 综合平衡 | 考虑同义词匹配，与人类判断相关性更高 |

### 4.4 主流评估框架对比

| 工具 | 核心机制 | 特色 | 适用阶段 |
|------|----------|------|----------|
| **RAGAS** | LLM 驱动，分析 Q-A-C 三元关系 | 合成数据生成、无参考评估 | 独立标准化评测 |
| **LlamaIndex Evaluation** | 嵌入式评估器，异步引擎 | 与开发流程深度集成 | 开发迭代验证 |
| **Phoenix (Arize)** | 分布式追踪 + 向量聚类 | 根因分析、Bad Case 诊断 | 生产环境监控 |

**最佳实践：** 开发阶段用 LlamaIndex 快速迭代 → RAGAS 标准化评测 → 上线后 Phoenix 监控诊断

### 4.5 企业级可观测性三维度

| 维度 | 监控指标 | SLO 示例 |
|------|----------|----------|
| **检索质量** | Recall@k, Precision@k, 语料覆盖率 | p95 recall@5 > 0.8 |
| **生成质量** | 忠实度、完整性、答案相关性 | 幻觉率 < 5% |
| **业务影响** | 问题解决率、首次生成可用率、CSAT | 解决率 > 85% |

---

## 五、高级 RAG 范式

### 5.1 知识图谱增强 RAG（KG-RAG）

传统 RAG 在多跳查询中的核心局限：关系理解缺失、上下文碎片化、跨文档联结能力弱、实体歧义。KG-RAG 将检索目标从独立文本片段转变为知识图谱中的实体、关系和子图。

#### 三阶段架构

```
知识图谱构建 → 图谱检索 → 增强生成
```

**知识图谱构建流程：**
1. **知识抽取：** LLM/IE 管线抽取实体、关系、属性（含指代消解、别名归一）
2. **质量控制：** 置信度评估、人机协同、本体约束校验
3. **图谱融合：** 实体对齐去重、跨来源合并、保留来源/时间戳
4. **存储索引：** Neo4j / NebulaGraph / TigerGraph / Neptune + 必要索引

**图谱检索策略（混合为主流）：**
1. 实体定位：实体链接或向量检索锁定核心实体节点
2. 子图探索：Cypher 查询 + 图遍历 + 邻域扩展 + 约束过滤
3. 结构化证据抽取：路径和节点属性序列化为可读证据

#### 方法论三分类

| 类型 | 特点 | 优劣 |
|------|------|------|
| **知识驱动型** | 完全依赖知识图谱查询推理 | 高精度、高可解释，但覆盖受限于图谱完备性 |
| **索引驱动型** | 图结构信息融入文本索引 | 集成成本低，但显式推理弱 |
| **混合型** | 图检索 + 文本检索，统一重排融合 | 综合表现最稳健，但系统复杂度高 |

#### 代表性框架

| 框架 | 核心思路 | 优势 | 劣势 |
|------|----------|------|------|
| **GraphRAG** (Microsoft) | 文本→三元组→Leiden 社区检测→分层摘要→全局+局部双视角 | 强全局感知 | 构建成本高，更新困难 |
| **LightRAG** | 双层检索+图增强索引 | 轻量高效 | 全局感知弱于 GraphRAG |
| **FRAG** | 查询分流：简单→实体链接，复杂→多跳推理 | 模块化，自适应 | 分类器精度决定上限 |
| **KG-IRAG** | 多轮图查询，逐步补齐证据链 | 鲁棒性强 | 延迟开销大 |

---

### 5.2 Self-RAG（自反思 RAG）

**核心思想：** 模型自身决定是否检索、评估检索质量、自我批判生成内容。

**机制：**
- 训练专用的 Reflect Token：`[Retrieval]/[No Retrieval]`、`[Relevant]/[Irrelevant]`、`[Grounded]/[Not Grounded]`、`[Utility:1-5]`
- 两阶段训练：Critic 模型学习评估 → Generator 学习在反思 Token 引导下生成

**核心优势：** 幻觉率显著低于其他方法，在临床案例测试中幻觉率 5.8%（次优方法 10.5%）

---

### 5.3 Corrective RAG（CRAG，修正 RAG）

**核心思想：** 检索后先评分，再决定是否修正，而非盲目喂给生成器。

**工作流：**
```
检索 → 评估（得分 -1 到 1） → 决策：
  ├─ 正确 → 直接使用
  ├─ 模糊 → 分解-重组
  └─ 错误 → 触发 Web 搜索回退
```

- 使用轻量 T5-large 评估器进行文档相关性评分
- 趋势：将 CRAG 集成到更大的 Agentic 框架中

---

### 5.4 Adaptive RAG（自适应 RAG）

**核心思想：** 不同复杂度的查询使用不同的检索策略，动态分配计算资源。

| 复杂度 | 策略 | 示例 |
|--------|------|------|
| **简单** | 无需检索 | "什么是 Python？" |
| **中等** | 单次检索 | "解释 Python 装饰器" |
| **复杂** | 多步检索 | "比较 async vs threading 在 I/O 场景的性能" |

- T5-large 分类器动态选择检索深度
- 简单查询绕过复杂检索，节省约 40% token

---

### 5.5 Agentic RAG（智能体 RAG）

**2025-2026 年前沿范式。** LLM 从"端点"升级为"协调者"。

**三大核心属性：**
1. **自主策略选择：** Agent 动态选择向量搜索、关键词、SQL、API、Web 搜索
2. **迭代执行：** 多轮检索 + 充分性评估（ReAct 思考-行动-观察循环）
3. **交叉工具使用：** 检索、计算、API 调用、推理交织进行

**代表系统：**
- **RAG-Critic** (ACL 2025)：批判引导的 Agentic 工作流，分级错误系统引导自我修正
- **ADORE** (2026)：记忆库 + 声明-证据图 + 证据覆盖引导停止条件

---

### 5.6 Multi-Agent RAG（多智能体 RAG）

解决单 Agent 在推理深度和速度上的瓶颈。

**典型架构（HM-ARAG）：**
```
Planner Agent → 分解复杂查询
Retrieval Agent → 多路并行检索
Tool Agent → 执行 SQL/API/Code
Reflection Agent → 验证输出质量
```

**关键特征：** 去中心化决策、并行检索、动态角色分配

---

## 六、RAG 流程全景图

```
┌──────────────────────────────────────────────────────────────────────┐
│                        离线索引阶段（Offline）                         │
│                                                                        │
│  数据加载 ──→ 文本分块 ──→ 向量嵌入 ──→ 向量数据库 ──→ 索引优化       │
│  (LlamaParse)  (Recursive/   (BGE-M3/     (Milvus/    (SentenceWindow │
│   Unstructured)  Semantic)    CLIP)        FAISS)      +元数据过滤)   │
│                                  │                                     │
│                 多模态嵌入 ←──────┤                                     │
│                  (CLIP/Gemini)    │                                     │
│                                  │                                     │
│                 知识图谱 ←────────┘                                     │
│                  (Neo4j+GraphRAG)                                       │
└──────────────────────────────────────────────────────────────────────┘
                                   ↓
┌──────────────────────────────────────────────────────────────────────┐
│                        在线查询阶段（Online）                          │
│                                                                        │
│  查询重构 ──→ 查询构建 ──→ 查询路由                                   │
│  (HyDE/       (SelfQuery/   (LLM路由/                                  │
│   MultiQuery)  Text2SQL/     RunnableBranch)                           │
│                Text2Cypher)                                            │
│                                  ↓                                     │
│  混合检索 ──→ 重排序 ──→ 上下文压缩                                   │
│  (BM25+Dense  (CrossEncoder/ (LLMChainFilter/                          │
│   +RRF融合)    ColBERT)      EmbeddingsFilter)                         │
│                                  ↓                                     │
│  格式化生成 ──→ 答案输出                                              │
│  (PydanticParser/Function Calling)                                     │
│                                  ↓                                     │
│  多维评估 ──→ 监控告警 ──→ 持续优化                                   │
│  (RAGAS+LlamaIndex+Phoenix)                                           │
└──────────────────────────────────────────────────────────────────────┘

高级分支：
  ├─ KG-RAG:      知识图谱构建 → 图检索 → 增强生成
  ├─ Self-RAG:    自反思检索决策 → 自批判生成
  ├─ CRAG:        检索 → 评估 → 修正/回退
  ├─ Adaptive:    查询复杂度分类 → 动态检索深度
  ├─ Agentic:     自主策略选择 → 多轮迭代 → 工具调用
  └─ Multi-Agent: 多智能体协作 → 并行检索 → 交叉验证
```

---

## 七、技术选型速查表

### 7.1 各阶段首选与备选

| 阶段 | 优先推荐 | 备选方案 |
|------|----------|----------|
| **数据加载** | LlamaParse（付费）/ Unstructured（开源） | Docling, Marker, MinerU, FireCrawl |
| **文本分块** | RecursiveCharacterTextSplitter → 层次化父子分块 | SemanticChunker, Late Chunking |
| **嵌入模型** | BGE-M3（自部署）/ Gemini Embedding 2（API） | Qwen3-Embedding-8B, OpenAI 3-large |
| **向量数据库** | Milvus（大规模）/ FAISS（小规模） | Qdrant, Elasticsearch, Pinecone |
| **索引优化** | Sentence Window + 元数据过滤 | 递归检索 |
| **查询优化** | HyDE + Multi-Query | Step-Back, 提示工程 |
| **混合检索** | BM25 + Dense, RRF 融合, α=0.7 | 加权线性组合 |
| **重排序** | ColBERT（精度效率平衡） | Cross-Encoder（最高精度）, RankLLM |
| **上下文压缩** | EmbeddingsFilter（低成本） | LLMChainExtractor, LLMChainFilter |
| **格式化生成** | PydanticOutputParser / Function Calling | JSON Schema 注入, GBNF 约束 |
| **评估框架** | RAGAS + LlamaIndex Eval | Phoenix（生产监控） |
| **高级范式** | 按需：Agentic RAG / KG-RAG / CRAG | Self-RAG, Adaptive RAG |

### 7.2 按场景选型

| 场景 | 核心方案 |
|------|----------|
| **企业知识库问答** | Advanced RAG + 层次化分块 + 混合检索 + Cross-Encoder 重排 |
| **多跳推理/复杂分析** | Agentic RAG 或 KG-RAG（需可解释性时） |
| **客服系统** | Adaptive RAG + 查询意图路由 + 缓存 |
| **合规/法律** | KG-RAG + 溯源追踪 + 安全护栏 |
| **多语言场景** | BGE-M3 嵌入模型 |
| **成本敏感** | BGE-M3 自部署 + FAISS + 缓存策略 |

---

## 八、RAG 实施路线图

```
阶段 1: 基础搭建（1-2 周）
  ├─ RecursiveCharacterTextSplitter，600-800 tokens
  ├─ BGE-M3 嵌入 + Milvus/FAISS
  ├─ 稠密向量检索
  └─ 基础评估（RAGAS）

阶段 2: 优化迭代（2-4 周）
  ├─ 加入 BM25 混合检索 + RRF 融合
  ├─ 层次化父子分块
  ├─ HyDE 查询增强
  └─ Cross-Encoder 重排序

阶段 3: 企业级加固（1-2 月）
  ├─ 分层知识库设计（核心静态/频繁更新/实时层）
  ├─ 多级缓存（查询结果 + 向量片段）
  ├─ 安全护栏（输入过滤 + 来源白名单 + 生成后验证）
  ├─ 可观测性（检索/生成/业务三维 SLO）
  └─ 持续更新机制（增量 + 全量 + 快照回滚）

阶段 4: 高级范式（按需）
  ├─ 多跳推理 → Agentic RAG
  ├─ 合规可解释 → KG-RAG (GraphRAG/LightRAG)
  ├─ 幻觉控制 → Self-RAG 或 CRAG
  └─ 成本优化 → Adaptive RAG + 轻量路由模型

阶段 5: 持续演进
  ├─ 建立 Bad Case 反馈闭环
  ├─ 数据飞轮驱动模型微调
  └─ 按业务指标而非技术指标衡量 ROI
```

---

> **核心启示：** RAG 是一门产品工程学科，而非模型技巧。将分块策略、索引质量、检索 SLO、评估体系和治理机制作为一等产品决策来对待的团队，是在生产环境中真正取得成功的团队。

> 本文档基于文件夹内全部 PDF 文档内容核实，结合 2025-2026 主流实践整理。
