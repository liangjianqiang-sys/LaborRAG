# LaborRAG 项目知识文档

本文档记录 LaborRAG 项目所涉及的全部技术知识，包括核心概念、算法原理、模型选型、架构设计等。

---

## 一、RAG（检索增强生成）

### 1.1 基本原理

RAG（Retrieval-Augmented Generation）将信息检索与大语言模型生成相结合，解决 LLM 的两大缺陷：

- **知识过时**：LLM 训练数据有截止日期，无法回答最新信息
- **幻觉问题**：LLM 可能编造不存在的事实，在法律场景下尤其危险

RAG 工作流程：

```
用户提问 → 检索相关文档 → 将文档作为上下文注入Prompt → LLM基于上下文生成回答
```

### 1.2 本项目的 RAG 演进

| 版本 | 检索策略 | 生成策略 | 核心改进 |
|------|---------|---------|---------|
| V1 | 纯向量检索 | LCEL 单次生成 | 基础 RAG 闭环 |
| V2 | 混合检索 + Reranker | LCEL 单次生成 | 检索精度提升 |
| V3 | 混合检索 + Reranker | CRAG 自我纠错 | 生成质量自检 |

---

## 二、文本向量化（Embedding）

### 2.1 原理

将文本映射为高维向量（如 1024 维），语义相近的文本在向量空间中距离更近。通过计算向量相似度（余弦相似度 / 内积）实现语义检索。

### 2.2 模型选型：BGE-M3

| 属性 | 值 |
|------|-----|
| 全称 | BAAI/bge-m3 |
| 维度 | 1024 |
| 语言 | 多语言（中英文优秀） |
| 特点 | 多功能（稠密检索 + 稀疏检索 + 多向量检索） |
| 大小 | 约 2GB |
| 运行方式 | 本地 CPU 推理 |

选择理由：
- 中文法律文本语义匹配效果好
- 本地运行，无 API 调用成本
- HuggingFace 开源，支持镜像加速下载

### 2.3 FAISS 向量库

FAISS（Facebook AI Similarity Search）是高效的向量相似度搜索库：

- **索引类型**：`IndexFlatIP`（内积搜索），配合 L2 归一化等价于余弦相似度
- **持久化**：`save_local` / `load_local` 序列化到磁盘
- **限制**：底层 C++ 不支持中文路径，向量库需存放在纯英文路径

---

## 三、检索策略

### 3.1 向量检索（V1）

```
Query → Embedding → FAISS 相似度搜索 → Top-K 文档
```

优点：语义理解能力强，"加班费"能匹配到"延长工作时间工资报酬"
缺点：关键词精确匹配弱，法条编号检索效果差

### 3.2 BM25 关键词检索

BM25 是基于词频的经典信息检索算法：

```
Score(D,Q) = Σ IDF(qi) · (f(qi,D) · (k1+1)) / (f(qi,D) + k1 · (1-b+b·|D|/avgdl))
```

- `f(qi,D)`：词 qi 在文档 D 中的词频
- `IDF(qi)`：逆文档频率，衡量词的区分度
- `b`：文档长度归一化参数（通常 0.75）
- `k1`：词频饱和参数（通常 1.2~2.0）

中文分词使用 jieba，首次加载约 1.5 秒构建前缀词典。

优点：关键词精确匹配强，法条编号、专有名词检索效果好
缺点：无语义理解，"加班费"无法匹配到"延长工作时间工资报酬"

### 3.3 混合检索 + RRF 融合（V2）

将向量检索和 BM25 检索结果通过 RRF（Reciprocal Rank Fusion）融合：

```
RRF_score(d) = Σ w_i / (k + rank_i(d) + 1)
```

- `rank_i(d)`：文档 d 在第 i 路检索中的排名
- `k`：RRF 常数（默认 60），防止排名靠前的结果权重过大
- `w_i`：各路权重（默认向量 0.5，BM25 0.5）

优势：兼顾语义匹配和关键词精确匹配，互补效果显著。

### 3.4 Reranker 重排序（V2）

在混合检索后，使用 Cross-Encoder 对 Top-K 结果精排：

**Bi-Encoder vs Cross-Encoder**：

| 对比 | Bi-Encoder | Cross-Encoder |
|------|-----------|---------------|
| 输入 | Query 和 Doc 分别编码 | Query 和 Doc 拼接后编码 |
| 速度 | 快（可预计算 Doc 向量） | 慢（每对都要前向传播） |
| 精度 | 中等 | 高 |
| 用途 | 初筛（召回） | 精排（重排序） |

本项目使用 `BAAI/bge-reranker-v2-m3`（约 560MB），对混合检索的 Top-8 结果重排序取 Top-5。

---

## 四、CRAG 自我纠错工作流（V3）

### 4.1 核心思想

传统 RAG 是"检索→生成"的单向流程，如果检索质量差，生成的回答也会出错。CRAG（Corrective RAG）在流程中引入**评估和纠错机制**：

1. 检索后评估文档质量，不精确则改写查询重新检索
2. 生成后评估回答忠实度，不忠实则重新生成

### 4.2 LangGraph 状态图

```
                    ┌──────────────┐
                    │   retrieve   │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │grade_documents│
                    └──────┬───────┘
                      ┌────┴────┐
                 precise    vague/irrelevant
                      │         │
                      │    ┌────▼─────┐
                      │    │rewrite_  │
                      │    │query     │
                      │    └────┬─────┘
                      │         │（回到retrieve）
                ┌─────▼──────┐  │
                │  generate  │◄─┘
                └──────┬─────┘
                       │
                ┌──────▼──────┐
                │grade_answer │
                └──────┬──────┘
                  ┌────┴────┐
             faithful   unfaithful
                  │         │
                  │    ┌────▼─────┐
                  │    │generate  │（重新生成，最多1次）
                  │    └────┬─────┘
           ┌──────▼──────┐  │
           │    END      │◄─┘
           └─────────────┘
```

### 4.3 检索评估（三分法）

| 评级 | 含义 | 路由 |
|------|------|------|
| `precise` | 文档精准命中问题核心，可直接回答 | → 生成 |
| `vague` | 文档与问题沾边但不够精确 | → 触发 Query 改写 |
| `irrelevant` | 文档与问题完全不相关 | → 触发 Query 改写 |

### 4.4 Query 改写

当检索评估为 vague/irrelevant 时，将模糊问题改写为精确的专业查询：

- "工资怎么算" → "劳动法中工资计算标准和加班工资计算方法"
- "那个赔偿金怎么规定的" → "劳动合同法中违法解除劳动合同的赔偿金规定"

改写后重新检索，最多改写 1 次（`MAX_REWRITES=1`）。

### 4.5 回答忠实度评估

判断生成的回答是否忠实于检索到的参考资料：

| 评级 | 含义 | 路由 |
|------|------|------|
| `faithful` | 回答观点有依据，合理概括 | → 输出 |
| `unfaithful` | 回答编造了参考资料中不存在的重要内容 | → 重新生成 |

重新生成最多 1 次（`MAX_REGENERATES=1`）。

### 4.6 多轮对话上下文

CRAG 支持多轮对话，将历史问答作为上下文传入：

```
[用户] 加班工资怎么算？
[AI] 根据《劳动法》第四十四条...
[用户] 法定节假日呢？    ← 依赖上一轮上下文
```

上下文格式：将历史问答拼接为字符串，注入改写和生成的 Prompt。

---

## 五、文档切分策略

### 5.1 递归切分（Recursive）

按固定长度切分，保留重叠：

```
CHUNK_SIZE=800, CHUNK_OVERLAP=100
```

缺点：可能在法条中间截断，破坏语义完整性。

### 5.2 法条结构化切分（law_article）

按法律条文（第X条）边界切分，保持每条法律完整：

1. 正则匹配 `第[一二三四五六七八九十百千]+条` 定位条文边界
2. 每个条文作为一个独立文档
3. 超长条文（>800字）再按段落细分

本项目默认使用 `law_article` 策略，保证法律条文的完整性。

---

## 六、RAGAS 评估体系

### 6.1 评估指标

| 指标 | 含义 | 评估什么 |
|------|------|---------|
| Faithfulness | 忠实度 | 回答是否忠实于检索上下文 |
| Answer Relevancy | 回答相关性 | 回答是否切题 |
| Context Precision | 上下文精确度 | 检索结果中相关内容的排名 |
| Context Recall | 上下文召回率 | 标准答案所需信息是否被检索到 |

### 6.2 评估数据集

20 个手工标注的问答对，覆盖劳动合同、工资、工伤、休假等主题，每对包含：
- 问题（question）
- 标准答案（ground_truth）
- 对应法条引用

### 6.3 对比实验

通过切换 `RETRIEVER_TYPE` 和 `RAG_MODE` 配置，运行 V1/V2/V3 三组实验：

| 实验 | 配置 | 说明 |
|------|------|------|
| V1 | `vector` + `simple` | 纯向量检索基线 |
| V2 | `reranked` + `simple` | 混合检索+重排序 |
| V3 | `reranked` + `crag` | CRAG 自我纠错 |

---

## 七、LLM 大语言模型

### 7.1 模型选型

本项目使用阿里云百炼平台的 LLM API：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| LLM_API_BASE | 百炼 API 地址 | OpenAI 兼容接口 |
| LLM_MODEL_NAME | qwen3.5-flash | 快速模型，适合 CRAG 多次调用 |
| LLM_TEMPERATURE | 0.1 | 低温度保证输出稳定 |
| LLM_MAX_TOKENS | 2048 | 最大输出长度 |

### 7.2 CRAG 中的 LLM 调用

CRAG 每次提问涉及 3-4 次 LLM 调用：

| 步骤 | 用途 | max_tokens | 超时 |
|------|------|-----------|------|
| 检索评估 | 判断 precise/vague/irrelevant | 64 | 60s |
| 生成回答 | 基于上下文生成专业回答 | 2048 | 60s |
| 忠实度评估 | 判断 faithful/unfaithful | 64 | 60s |
| Query改写（可选） | 改写模糊问题 | 128 | 60s |

### 7.3 Token 消耗优化

| 策略 | 效果 | 本项目实现 |
|------|------|-----------|
| 精简 Prompt | 每次省约 200 tokens | ✅ 生成 Prompt 精简为 1 句规则 |
| 限制检索片段数 | 减少输入上下文 | ✅ Rerank Top-5，评估取 Top-3 |
| 截断文档内容 | 评估时不传全文 | ✅ 评估时文档截取 150-500 字 |
| 截断评估回答 | 忠实度评估不传全文 | ✅ 回答截取前 1000 字 |
| Rerank 重排序 | 过滤无关文档 | ✅ bge-reranker-v2-m3 |

---

## 八、前端技术

### 8.1 技术栈

| 技术 | 用途 |
|------|------|
| React 18 | UI 框架 |
| TypeScript | 类型安全 |
| TailwindCSS | 原子化 CSS 样式 |
| React Router | 多页面路由 |
| Vite | 构建工具 |
| Lucide React | 图标库 |
| ReactMarkdown | Markdown 渲染 |

### 8.2 多页面布局

| 页面 | 路由 | 功能 |
|------|------|------|
| ChatPage | / | 智能问答 |
| KnowledgePage | /knowledge | 知识库管理 |
| EvalPage | /evaluation | RAGAS 评估 |
| SettingsPage | /settings | 系统设置 |

### 8.3 关键交互设计

- **ChatPage 始终挂载**：通过 CSS `display` 控制显隐，避免路由切换时丢失聊天状态
- **参考来源折叠**：默认收起，点击展开，避免长文档占据界面
- **CRAG 步骤可视化**：折叠面板展示工作流步骤（检索→评估→生成→评估）
- **加载动画**：检索时显示"正在检索法律条文..."动画

---

## 九、后端技术

### 9.1 技术栈

| 技术 | 用途 |
|------|------|
| FastAPI | Web 框架 |
| Uvicorn | ASGI 服务器 |
| LangChain 1.0 | RAG 编排框架 |
| LangGraph | CRAG 状态图 |
| FAISS | 向量存储 |
| sentence-transformers | Embedding + Reranker |
| jieba | 中文分词 |
| Pydantic | 数据校验 |
| python-dotenv | 环境变量 |

### 9.2 API 设计

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/v1/chat` | 智能问答（同步，线程池运行） |
| GET | `/api/v1/knowledge-base/status` | 知识库状态 |
| POST | `/api/v1/knowledge-base/build` | 构建知识库 |
| POST | `/api/v1/documents/upload` | 上传文档 |
| GET | `/api/v1/documents/list` | 文档列表 |
| DELETE | `/api/v1/documents/{filename}` | 删除文档 |
| POST | `/api/v1/evaluation/run` | 运行 RAGAS 评估 |
| GET | `/api/v1/evaluation/report` | 获取评估报告 |

### 9.3 关键设计决策

- **`/chat` 使用同步 `def`**：CRAG 的 `graph.invoke()` 是同步阻塞调用，使用 `async def` 会阻塞事件循环导致其他 API 无响应。FastAPI 对同步 `def` 自动在线程池中运行。
- **CRAG 降级机制**：CRAG 工作流执行失败时，自动降级到 simple 模式（直接检索+生成），保证用户始终能得到回答。
- **模型懒加载**：Reranker 模型首次提问时才加载，避免启动时长时间等待。
- **HuggingFace 镜像**：国内环境设置 `HF_ENDPOINT=https://hf-mirror.com` 加速模型下载。

---

## 十、法律知识库

### 10.1 覆盖法律

| 法律文件 | 主要内容 |
|---------|---------|
| 《劳动法》 | 工时、工资、劳动保护等基本制度 |
| 《劳动合同法》 | 劳动合同订立、履行、解除与经济补偿 |
| 《劳动争议调解仲裁法》 | 劳动争议处理程序 |
| 《社会保险法》 | 养老、医疗、工伤、失业、生育保险 |
| 《工伤保险条例》 | 工伤认定、劳动能力鉴定、工伤保险待遇 |
| 《职工带薪年休假条例》 | 年休假天数、安排与补偿 |

### 10.2 典型问答场景

| 场景 | 示例问题 | CRAG 行为 |
|------|---------|----------|
| 精确查询 | "加班工资怎么算" | precise → 直接生成 |
| 模糊查询 | "工资怎么算" | vague → 改写 → 重新检索 → 生成 |
| 多轮追问 | "法定节假日呢" | 结合上下文理解意图 |
| 代词指代 | "那个赔偿金怎么规定的" | 改写为完整问题后检索 |
| 知识库外 | "远程办公有什么规定" | irrelevant → 改写 → 仍无精确结果 → 基于现有资料回答 |
