# LaborRAG 后续开发规划

> 基于《RAG完整流程与主流方法总结.md》与项目现状的差距分析，按优先级排列。

---

## 现状对照

| 文档阶段 | 文档内容 | 项目现状 | 差距 |
|----------|----------|----------|------|
| 阶段1 基础搭建 | 递归分块 + BGE-M3 + 稠密检索 + RAGAS | ✅ V1已完成 | 无 |
| 阶段2 优化迭代 | BM25混合 + 父子分块 + HyDE + Cross-Encoder重排 | ✅ V2/V3已完成 | 无 |
| 阶段3 企业级加固 | 分层知识库 + 多级缓存 + 安全护栏 + 可观测性 + 持续更新 | ⚠️ 部分完成 | 可观测性✅，其余缺失 |
| 阶段4 高级范式 | Agentic RAG / KG-RAG / Self-RAG / CRAG / Adaptive | ⚠️ V4代码完成，未测试 | V4待测试验证 |
| 阶段5 持续演进 | Bad Case闭环 + 数据飞轮 + 业务指标ROI | ❌ 未开始 | Bad Case有展示无闭环 |

---

## 优先级 P0：必须完成（毕设核心交付物）

### 1. V4 端到端测试
**对应文档**：5.5 Agentic RAG 三大核心属性验证
**当前状态**：README 9项测试标准全部 ⬜

测试项：
- [ ] Router意图分类准确（法条查询/计算类/对比类正确分发）
- [ ] Calculator精确计算（加班费/经济补偿金/赔偿金等；缺参时返回公式框架）
- [ ] Comparator对比分析（双组检索+结构化对比表）
- [ ] Retriever路径正常（走V3检索+压缩链路，5条规则防幻觉）
- [ ] Validator验证生效（检测幻觉+法条不在资料中并修正）
- [ ] 多Agent协作完整（Router→专业Agent→Validator全链路）
- [ ] Agent降级兜底（异常时自动降级到simple模式）
- [ ] 评估系统异步运行（后台线程+前端轮询+页面恢复状态）
- [ ] 前端Agent步骤可视化

**验证方法**：逐项设计测试问题，记录通过/失败

### 2. V1-V4 四版对比实验
**对应文档**：4.1-4.5 评估体系 + 8.实施路线图
**当前状态**：评估系统已就绪（三元组+检索+响应+可观测性+Bad Case），但实验未运行

实验方案：
| 版本 | RAG模式 | 检索策略 | 评估重点 |
|------|---------|---------|---------|
| V1 | simple | vector | 基线：纯向量检索+简单生成 |
| V2 | simple | reranked | 检索增强：混合+重排序提升检索指标 |
| V3 | crag | reranked | 纠错能力：CRAG提升忠实度 |
| V4 | agent | reranked | 端到端：Agent提升计算/对比类指标 |

**预期产出**：
- 四版三元组雷达图对比
- 检索指标(P/R/F1/MRR/MAP)趋势
- 响应指标(ROUGE-L/BLEU/幻觉率/完整性)趋势
- 按问题类型(retrieve/calculate/compare)分项对比
- Bad Case分析报告

---

## 优先级 P1：增强能力（提升系统完整性）

### 3. 查询重构增强
**对应文档**：3.1 查询重构
**当前差距**：项目仅有 HyDE，文档还推荐 Multi-Query / Step-Back Prompting

- **Multi-Query 分解**：将复杂问题拆为多个子问题分别检索后合并去重
  - 适用：包含多个子主题的问题（如"加班费和年休假怎么算"）
  - 实现：LLM生成3-5个子查询 → 并行检索 → RRF融合去重
- **Step-Back Prompting**：先生成高层次"退步问题"检索通用原理，再具体推理
  - 适用：需要原理支撑的细节问题
  - 实现：两阶段检索（通用原理 + 具体细节）

### 4. Self-Query 元数据过滤检索
**对应文档**：3.2 查询构建 — 文本→元数据过滤器
**当前差距**：项目有元数据（source/法条编号），但检索时未利用SelfQuery自动提取过滤条件

- 用户问"劳动合同法第47条"时，应自动提取 `source=劳动合同法` + `article=第47条` 过滤
- 当前靠BM25关键词匹配，精度有限
- 实现：LLM提取结构化过滤条件 → FAISS metadata filter → 缩小检索范围

### 5. Bad Case 反馈闭环
**对应文档**：8.阶段5 持续演进 — 建立 Bad Case 反馈闭环
**当前差距**：前端有Bad Case展示，但无闭环机制（分析→修复→验证）

闭环流程：
```
Bad Case展示 → 人工标注问题根因（检索不足/生成幻觉/计算错误/对比遗漏）
    → 针对性修复（补充法条/Prompt优化/计算公式修正）
    → 重新评估验证改进效果
```
- 前端增加"根因标注"功能
- 后端存储根因标签，支持按根因类型筛选
- 修复后一键重跑评估验证

---

## 优先级 P2：锦上添花（时间允许再做）

### 6. 多级缓存与成本优化
**对应文档**：8.阶段3 — 多级缓存（查询结果 + 向量片段）

- L1 语义缓存：相似问题直接返回缓存答案（Embedding相似度>0.95）
- L2 检索缓存：相同查询缓存检索结果，跳过检索环节
- 预期收益：高频问题响应时间 -60%，token消耗 -40%

### 7. 安全护栏增强
**对应文档**：8.阶段3 — 安全护栏（输入过滤 + 来源白名单 + 生成后验证）

- 输入过滤：检测并拒绝非法/超出领域的问题
- 来源白名单：Validator验证引用法条是否在知识库文档列表中
- 当前Validator只检查上下文中是否出现，不检查是否在完整知识库中

---

## 执行顺序建议

```
Step 1: V4端到端测试（1-2天）
  ↓ 修复测试中发现的问题
Step 2: V1-V4四版对比实验（1天运行 + 1天分析）
  ↓ 整理实验数据，撰写分析报告
Step 3: 查询重构增强（Multi-Query优先，2-3天）
  ↓ 可选：Step-Back Prompting
Step 4: Self-Query元数据过滤（1-2天）
Step 5: Bad Case闭环（1-2天）
  ↓
Step 6-7: 缓存/安全护栏（时间允许）
```

---

## 与文档实施路线图的映射

| 文档路线图 | 项目对应 | 状态 |
|-----------|---------|------|
| 阶段1: 基础搭建 | V1 | ✅ 完成 |
| 阶段2: 优化迭代 | V2+V3 | ✅ 完成 |
| 阶段3: 企业级加固 | 可观测性✅ / 缓存❌ / 安全护栏⚠️ / 分层知识库❌ | 部分完成 |
| 阶段4: 高级范式 | Agentic RAG(V4)✅代码 / KG-RAG❌ / Self-RAG❌ | 代码完成待测试 |
| 阶段5: 持续演进 | Bad Case闭环❌ / 数据飞轮❌ | 未开始 |

**核心结论**：项目已覆盖文档阶段1-2全部内容和阶段4的Agentic RAG，阶段3的可观测性已完成。后续重点应放在 **V4测试验证 + 四版对比实验**（毕设核心交付物），然后按需补充查询重构和Bad Case闭环。
